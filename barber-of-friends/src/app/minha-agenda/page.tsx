import { redirect } from "next/navigation";
import { getSession } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
export default async function MinhaAgendaPage(){const session=await getSession();if(!session)redirect("/login?next=/minha-agenda");const appointments=await prisma.$queryRaw<Array<{id:number;appointment_date:string;start_time:string;final_price:number;status:string;service_name:string}>>`SELECT a.id,DATE_FORMAT(a.appointment_date,'%Y-%m-%d') AS appointment_date,TIME_FORMAT(a.start_time,'%H:%i') AS start_time,a.final_price,a.status,s.name AS service_name FROM appointments a INNER JOIN services s ON s.id=a.service_id WHERE a.client_id=${session.id} ORDER BY a.appointment_date ASC,a.start_time ASC`;const firstName=session.name.trim().split(/\s+/)[0];return <main className="site-shell dashboard-shell"><section className="page dashboard"><div className="dashboard-head"><div><a className="back-link" href="/">← Início</a><span className="eyebrow">BARBER OF FRIENDS • CLIENTE</span><h1 className="page-title">Olá, {firstName}.</h1><p className="page-subtitle">Aqui estão seus agendamentos.</p></div><div className="actions"><a className="btn btn-primary" href="/agendar">Novo agendamento</a><form action="/api/auth/logout" method="post"><button className="btn btn-secondary">Sair</button></form></div></div><section className="panel glass"><span className="eyebrow">MINHA AGENDA</span><h2>Meus horários</h2><div className="appointment-list">{appointments.length===0?<p className="page-subtitle">Você ainda não possui agendamentos.</p>:appointments.map(a=><div className="appointment" key={a.id}><div><strong>{a.service_name}</strong><span>{a.status} • R$ {Number(a.final_price).toFixed(2).replace(".",",")}</span></div><time>{a.appointment_date.split("-").reverse().join("/")} às {a.start_time}</time></div>)}</div></section></section></main>}
