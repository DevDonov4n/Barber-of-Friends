const particles = Array.from({ length: 70 }, (_, index) => ({ left: `${(index * 37) % 100}%`, top: `${(index * 61) % 100}%`, x: `${((index % 7) - 3) * 18}px`, y: `${((index % 5) - 2) * 20}px`, duration: `${4 + (index % 6)}s`, color: index % 2 === 0 ? "#ff304f" : "#2878ff" }));

export default function Home() { return <main className="site-shell"><div className="particle-field" aria-hidden="true">{particles.map((p, i) => <span key={i} className="particle" style={{ left:p.left, top:p.top, ["--x" as string]:p.x, ["--y" as string]:p.y, ["--duration" as string]:p.duration, ["--particle-color" as string]:p.color }} />)}</div><header className="navbar"><a className="brand" href="#inicio">BARBER<span>OF</span>FRIENDS</a><nav className="nav-links"><a href="#inicio">Início</a><a href="/agendar">Agendamento</a><a href="#sobre">Sobre</a><a href="#contato">Contato</a></nav><a className="btn btn-secondary" href="/login">Entrar</a></header><section className="hero" id="inicio"><span className="eyebrow">Barbearia • Agendamento inteligente</span><h1>Seu corte.<br/>Seu horário.<br/><span className="gradient-text">Sem complicação.</span></h1><p>Uma experiência rápida para encontrar seu horário, escolher o melhor dia e chegar na cadeira sem perder tempo.</p><div className="actions"><a className="btn btn-primary" href="/agendar">Agendar meu corte</a><a className="btn btn-secondary" href="#sobre">Conhecer a barbearia</a></div></section><section className="feature-grid" id="sobre"><article className="feature glass"><span className="number">01</span><h2>Horários de 30 minutos</h2><p>Escolha o dia e veja a disponibilidade em blocos de meia hora.</p></article><article className="feature glass"><span className="number">02</span><h2>Conta de cliente</h2><p>Cadastre-se uma vez e agilize seus próximos agendamentos.</p></article><article className="feature glass"><span className="number">03</span><h2>Painel do barbeiro</h2><p>Uma agenda centralizada para acompanhar os próximos atendimentos.</p></article></section><footer className="page" id="contato" style={{paddingTop:10}}><p style={{color:"var(--muted)",fontSize:".85rem"}}>© 2026 Barber of Friends — todos os direitos reservados.</p></footer></main>; }
